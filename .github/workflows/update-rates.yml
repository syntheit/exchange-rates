name: Rates Refresh
on:
  schedule:
    - cron: "0 8 * * *" # 8:00 AM UTC daily
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Refresh Rates
        env:
          EXCHANGE_KEY: ${{ secrets.EXCHANGE_KEY }}
        run: |
          go run src/main.go

      - name: Commit Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Rates refresh [skip ci]"
          file_pattern: "rates.json"
